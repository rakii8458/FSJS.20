let str= 'raksha   ';
console.log('lenght of the string', str.length);
//but the actual length is different , let's print the actual length

console.log('But the actual length is ', str.trim().length)